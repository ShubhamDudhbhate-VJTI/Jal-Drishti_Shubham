# ── Jal-Drishti Frontend — Production Environment Variables ──
# Set these in the Vercel dashboard under Settings → Environment Variables.
# This file is a TEMPLATE — do NOT put real keys here.

# Backend API URL — replace with your Railway/Render deployed URL
VITE_API_BASE_URL=https://your-backend-url.railway.app

# Supabase (anon key is safe for browser)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
